import React from 'react';
import './Loader.sass';
const Loader = (props) => {
    return (
        <div className={"Loader--center"}>
            <div className="Loader">

            </div>
        </div>
    );
};

export default Loader;
